# Software Registry

This folder is a shared knowledge base for tool setup and usage.

## How to use
- Add one file per tool in `docs/software/`.
- Update the file when you discover a working setup or a gotcha.

## Template
Copy `docs/software/template.md` for new entries.
