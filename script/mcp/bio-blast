#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${BIO_MCP_BLAST_DIR:-}" ]]; then
  echo "BIO_MCP_BLAST_DIR is not set."
  echo "Set BIO_MCP_BLAST_DIR to the path of your bio-mcp-blast repo."
  exit 1
fi

cd "$BIO_MCP_BLAST_DIR"
exec python -m src.server
