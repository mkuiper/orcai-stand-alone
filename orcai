#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec bun run --cwd "$root/packages/opencode" --conditions=browser "$root/packages/opencode/src/index.ts" "$@"
